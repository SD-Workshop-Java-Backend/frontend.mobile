import { Container, TextDelete } from "../styles/SwipeableRightStyle";

export default SwipeableRight = (progress, dragX) => {
    
    return(
        <Container>
            
            <TextDelete>
                Apagar
            </TextDelete>
            
        </Container>
    );
}
